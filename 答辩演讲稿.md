# 毕业答辩演讲稿
## 基于Raft算法的分布式数据库构建

### 开场（第1页，30秒）

尊敬的各位老师，大家好！我是张皓然，我的毕业设计题目是"基于Raft算法的分布式数据库构建"。接下来我将从研究背景、系统设计、功能实现等方面进行汇报。

### 目录（第2页，20秒）

我的汇报主要包括七个部分：首先介绍研究背景与目标，然后阐述相关理论基础，重点讲解Raft算法的核心机制，接着展示系统的设计架构和功能实现，最后进行性能分析并总结展望。

### 研究背景（第3页，40秒）

在大数据时代，数据呈现爆炸式增长。传统的单机数据库在容量、性能和可靠性方面都遇到了瓶颈，分布式架构成为必然选择。但在分布式环境中，如何保证数据一致性成为核心技术难题。当多个节点同时存储数据时，如何确保它们看到的数据是一致的？当部分节点发生故障时，如何保证系统继续正常运行？这些都是我们需要解决的关键问题。

### 研究目标（第4页，30秒）

因此，本项目的目标是构建一个基于Raft算法的分布式键值数据库。主要实现四个目标：
- 第一，实现强一致性的数据存储，保证所有节点数据一致
- 第二，保证系统高可用性，部分节点故障不影响服务
- 第三，支持节点故障后的自动恢复
- 第四，提供简洁易用的API接口

技术路线上，我们采用Raft共识算法作为核心，使用Go语言开发，并选择高性能的Kitex RPC框架进行节点间通信。

### CAP理论（第5页，40秒）

在分布式系统设计中，CAP理论是重要的理论基础。CAP代表三个特性：
- C是一致性，要求所有节点在同一时刻看到相同的数据
- A是可用性，要求系统持续提供服务
- P是分区容错性，要求系统在网络分区时仍能运行

CAP理论告诉我们，这三者不可能同时满足。本系统定位为CP系统，即优先保证数据一致性和分区容错性，在网络分区时可能牺牲部分可用性。

### 为什么选择Raft（第6页，40秒）

在众多共识算法中，我们为什么选择Raft？这里对比了三种主流算法：
- Paxos算法虽然理论完备，但过于复杂，难以理解和实现
- ZAB算法主要用于ZooKeeper，通用性不强
- Raft算法设计目标就是易理解性，将复杂问题分解为独立的子问题

Raft的优势在于算法清晰易懂、采用模块化设计、有广泛的工业应用，如etcd、TiKV等知名项目都基于Raft实现。

### Raft核心机制（第7页，40秒）

Raft算法中，节点有三种角色：
- Leader负责处理所有客户端请求，管理日志复制，通过心跳维持领导地位
- Follower是被动角色，接收Leader的日志，响应Leader请求
- Candidate是选举过程中的临时状态

整个系统在任一时刻最多只有一个Leader，这保证了决策的一致性。

### Leader选举（第8页，50秒）

Leader选举是Raft的核心机制之一。选举会在三种情况下触发：系统初始化、Leader故障、网络分区恢复。

选举过程中有两个关键设计：
- 随机超时机制：每个节点的选举超时时间是随机的，这避免了多个节点同时发起选举导致选票分散
- 任期机制：每次选举都会增加任期号，用于识别过期信息

当Follower在超时时间内没有收到Leader心跳，就会发起选举，如果获得多数票就成为新Leader。

### 日志复制（第9页，50秒）

日志复制是保证数据一致性的关键。流程如下：
1. 客户端发送写请求到Leader
2. Leader将操作追加到本地日志，然后并行复制到所有Follower
3. 当多数节点（包括Leader自己）确认接收后，Leader提交这条日志
4. 最后将操作应用到状态机，返回结果给客户端

这种基于多数派的机制保证了即使部分节点故障，已提交的数据也不会丢失。

### 系统架构（第10页，40秒）

系统采用分层架构设计：
- 接口层提供API和SDK，方便客户端调用
- 服务层处理业务逻辑，如请求路由、结果返回
- 共识层实现Raft算法，包括选举、日志复制等
- 存储层负责数据持久化，包括KV存储和预写日志

技术栈方面，我们使用Go语言开发，充分利用其并发特性；使用Kitex和Thrift实现高性能RPC通信；存储采用内存KV加WAL日志的方式。

### 功能实现（第11页，30秒）

系统实现了完整的键值存储功能，包括SET设置键值、GET获取值、DELETE删除键等基础操作。

在集群管理方面，支持节点动态加入和退出、自动故障检测与恢复、日志同步与一致性保证等功能。

### Leader选举测试（第12页，40秒）

接下来展示功能测试结果。首先是Leader选举测试：
我们模拟Leader节点故障的场景，关闭当前Leader后，观察集群的反应。测试结果显示，Follower 2节点在检测到Leader故障后，成功发起选举并当选为新的Leader，整个过程在秒级完成，证明了故障恢复机制的有效性。

### 日志同步测试（第13页，40秒）

日志同步测试中，我们在Leader上执行SET操作，然后观察所有Follower的日志输出。测试结果表明，Leader的操作成功同步到所有Follower节点，各节点的数据保持完全一致，验证了日志复制机制的正确性。

### 性能分析（第14页，50秒）

性能测试中，我们与单机Redis进行了对比。在10万次读操作的测试中：
- 不同负载下，DDBR的性能约为Redis的32-34%
- 性能差距主要来源于一致性保证的开销
- 每次操作都需要多数节点确认，增加了网络延迟

但这是合理的权衡——我们牺牲了部分性能，换取了强一致性和高可用性，这正符合CAP理论的预期。

### 系统优势（第15页，40秒）

本系统的技术优势包括：
- 基于Raft算法实现强一致性保证
- 支持自动故障恢复，提供高可用性
- 采用模块化设计，易于扩展和维护
- 使用高性能的Kitex RPC框架

创新点在于：完整实现了Raft的核心机制，优化了日志复制性能，并提供了多种一致性级别的读取策略，满足不同场景需求。

### 总结（第16页，40秒）

总结本次毕业设计，我完成了以下工作：
- 深入研究了Raft共识算法的原理
- 设计并实现了完整的分布式KV数据库系统
- 实现了Leader选举、日志复制等所有核心功能
- 通过测试验证了系统的正确性和性能

最终构建了一个可用的分布式存储系统，在一致性和可用性之间找到了平衡，为分布式系统开发提供了实践参考。

### 展望（第17页，40秒）

未来的改进方向包括三个方面：
- 性能优化：实现ReadIndex、LeaderLease等机制，进一步降低读延迟
- 功能扩展：增加事务支持、数据分片、更多数据类型
- 可靠性提升：完善成员变更、增强安全机制、支持跨数据中心部署

### 结束语（第18页，20秒）

以上就是我的毕业设计汇报，感谢各位老师的聆听！请各位老师批评指正，谢谢！

---

## 时间分配总结
- 开场与目录：50秒
- 背景与理论（第3-6页）：2分30秒  
- Raft算法详解（第7-9页）：2分20秒
- 系统设计与实现（第10-11页）：1分10秒
- 测试与性能（第12-14页）：2分10秒
- 总结与展望（第15-18页）：1分40秒
- **总计：约10分钟**

## 答辩提示
1. 语速适中，保持自信
2. 重点突出创新点和实现难点
3. 准备可能的提问，如：
   - Raft与Paxos的具体区别
   - 系统的容错能力如何
   - 如何处理脑裂问题
   - 性能优化的具体方案 